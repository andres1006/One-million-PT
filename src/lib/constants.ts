export const DEFAULT_PAGE_SIZE = 10;
export const AI_SUMMARY_HISTORY_LIMIT = 5;
export const STORAGE_KEYS = {
  leads: "omc.leads.v1",
  aiHistory: "omc.ai.history.v1",
  theme: "omc.theme.v1",
} as const;
